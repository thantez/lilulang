%% Single line comment\n

declare {
    %~
      Multi line
      commmmmmmmmmmmment
    ~%

    string b;

    myID(string[]a, bool x);

    (int) = func(float r);

    lol#;

    const string s[2+3] = 3*-8;
}

(int h) = function test() {
  b = 'salam\tbeshoma';

  int _c = 0Xa12;

  float #f = 12.1E-42;

  string __s# = 'Chetorii\rShoma?\' Tnx \XB3 \xf1';
  
  bool bb = true;
}

type my#Type_:tp {
  public const string v[allocate test()];
  protected int z;

  function funcID(string s1) {
    if (s1 == '\xD3') {
      this.z = 12;
    } else {
      this.z = 0x63;
    }
    return;
  }
}

function WT#(int incoming) {
  int sum = 0;
  for int i = 0; i < incoming; i = i + 1 {
    sum = incoming * sum - i / 51 % -5;
  }

  while incoming == 0xCc {
    switch super.dad {
      case 0x12: {
        destruct [] whatIsGoingOn;
      }
      case 156375: {
        my#Type wt = 13;
      }
      default: {
        incoming = 'sfawe\n\t\\';
      }
    }
  }
}